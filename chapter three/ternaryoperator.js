// ternary operator ? for online condition.

var num = 30;
var numone = 49;
var str = null;
var strone = null;

str = num % 2 == 0 ? 'even' : 'odd';
console.log(str);//even
strone = (numone % 2 == 0)? 'even' : 'odd';
console.log(strone);//odd 